# Railgun Docker Image

This repository contains a Dockerfile to run CloudFlare's Railgon on AusNimbus (OpenShift V3)

## Configuration

The following environment variables are required:

- TOKEN
- PUBLIC_HOSTNAME
- LOG_LEVEL
- MEMCACHED_HOST
- MEMCACHED_PORT
- MEMCACHED_LIMIT
